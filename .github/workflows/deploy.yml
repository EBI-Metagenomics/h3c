name: Deploy

on: push

jobs:
  deploy:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-22.04]

    steps:
      - uses: actions/checkout@v2

      - name: Install podman
        run: sudo apt-get install -y podman

      - name: Download h3daemon
        run: |
          curl https://raw.githubusercontent.com/EBI-Metagenomics/h3daemon/main/h3daemon --output h3daemon
          chmod +x h3daemon

      - name: Configure CMake
        run: cmake -B ${{github.workspace}}/build -DCMAKE_BUILD_TYPE=Release -DENABLE_ALL_WARNINGS=ON

      - name: Build
        working-directory: ${{github.workspace}}/build
        run: cmake --build . --config Release

      - name: Run h3daemon
        run: |
          ${{github.workspace}}/h3daemon ${{github.workspace}}/build/test/assets/minifam.hmm &
          sleep 15

      - name: Test
        working-directory: ${{github.workspace}}/build
        run: ctest -C Release

      - name: CPack
        working-directory: ${{github.workspace}}/build
        run: cpack

      - name: Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/v')
        with:
          files: |
            ${{github.workspace}}/build/package/*
